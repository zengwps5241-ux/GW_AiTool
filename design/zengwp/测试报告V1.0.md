# AI 顾问作战台 · 测试执行报告 V1.0

> **执行依据**：[测试用例V1.0.md](测试用例V1.0.md)（502 条用例 / 18 模块）
> **执行日期**：2026-07-15 ～ 2026-07-16
> **执行环境**：Windows 11 / Python 3.13.14 / PostgreSQL 16.14 / Node.js 24.15.0
> **执行范围**：后端 pytest 全量套件（815 用例）+ 前端 Node 单元测试（9 文件）+ 测试用例→自动化映射追溯
> **报告版本**：V1.0

---

## 一、执行结论（TL;DR）

| 维度 | 结果 |
|------|------|
| **后端自动化测试** | **789 passed / 21 failed / 2 skipped / 3 errors**（815 收集，通过率 96.8%） |
| **附录A 冒烟清单（P0 精选）后端可测部分** | **全部通过**（核心 P0 模块 0 失败） |
| **前端单元测试** | 5 passed / 4 无法运行（依赖 vitest 未安装） |
| **测试用例覆盖追溯** | 502 条中 236 条有自动化覆盖、51 条部分覆盖、215 条无自动化覆盖（UI/AI 执行类） |
| **真实产品缺陷** | **1 个待复核**（SSE 断开后后台 runner 收尾）+ 1 个契约漂移（历史消息窗口 10→200） |
| **可上线判定** | **后端核心业务闭环可上线**；需先修复 1 个待复核缺陷，并清理 14 条过时测试 |

> **核心结论**：21 个失败 + 3 个错误中，**无一属于核心业务逻辑真实缺陷**。它们全部归因于 4 类**测试环境/测试维护问题**（环境变量泄漏、企微旧代码测试未清理、Windows 路径符号、DB 口令不一致），仅 1 个 SSE 后台 runner 用例需人工复核确认是否为真实时序缺陷。附录 A 上线冒烟清单的后端可自动化部分**零失败**。

---

## 二、测试环境准备

### 2.1 环境核验

| 项 | 状态 | 说明 |
|----|------|------|
| Python venv | ✅ | `backend/.venv` Python 3.13.14，fastapi 0.136.1 / sqlalchemy 2.0.49 / pytest 9.0.3 |
| PostgreSQL | ✅ | 16.14，端口 5432 可达；库 `gokagent` / `gokagent_test` / `postgres` 均存在 |
| 测试库 `gokagent_test` | ✅ | 口令 `dev`（conftest.py 默认），连接正常 |
| Redis | ⚠️ 未单独验证 | 测试用 fakeredis 替代，不影响套件 |
| 前端依赖 | ✅ | `node_modules` 已安装；`tsc -b` 类型检查通过（exit 0） |

### 2.2 测试基础设施机制（来自 conftest.py）

- 每个 API 测试自动 DROP/CREATE `public` schema → 干净库隔离
- `app_env` fixture 按 6 层依赖顺序 `importlib.reload` 全部模块，避免跨用例状态污染
- 播种内置 3 角色 + 16 菜单 + 角色菜单关联（与生产 lifespan 一致）
- 提供 `client`（未登录）/ `logged_in_client`（user）/ `admin_client`（admin）/ `super_client`（super）四套夹具
- V2.2 认证重构后，测试改用自建注册登录（`_register_and_login`），企微流程已弃用

---

## 三、后端全量测试结果

### 3.1 总览

```
815 collected → 789 passed, 21 failed, 2 skipped, 3 errors
耗时：1848.97s（约 30 分钟）
```

### 3.2 失败/错误清单（24 项）及根因分类

| # | 测试 | 类型 | 根因分类 | 是否真实缺陷 |
|---|------|------|---------|------------|
| 1 | test_config::test_settings_defaults | 单元 | **环境变量泄漏**：shell 全局 `ANTHROPIC_MODEL=glm-5.2` 渗入 pytest，断言 `==""` 失败 | ❌ 环境 |
| 2 | test_config::test_model_providers_are_discovered_from_env | 单元 | 同上：多出 `DEFAULT` provider（因 ANTHROPIC_AUTH_TOKEN/BASE_URL/MODEL 三者非空触发 DEFAULT 分支） | ❌ 环境 |
| 3 | test_model_settings_api::test_model_settings_returns_flat_models_and_thinking_levels | 接口 | 同上：models 列表多出 `glm-5.2` | ❌ 环境 |
| 4 | test_wechat_work::test_get_access_token_success 等 5 条 | 单元 | **过时测试**：`wechat_work.py` 全文已注释（V2.2 弃用企微），`get_access_token` 等符号不存在 → ImportError | ❌ 测试维护 |
| 5 | test_departments_sync::test_sync_departments_* 等 6 条 | 单元 | **过时测试**：`departments.py` 全文已注释，`sync_departments`/`get_department_names` 不存在 → AttributeError/ImportError | ❌ 测试维护 |
| 6 | test_sessions_crud::test_session_isolation_between_users | 单元 | **过时测试**：patch `wechat_work.get_access_token`（已不存在）→ AttributeError | ❌ 测试维护 |
| 7 | test_messages_api::test_messages_calls_load_history | 接口 | **Windows 路径分隔符**：实际 `user_workspaces\alice`，断言 `endswith("user_workspaces/alice")` | ❌ 平台 |
| 8 | test_sessions_crud::test_delete_triggers_remove_session | 单元 | 同上：Windows 反斜杠路径 | ❌ 平台 |
| 9 | test_workspace_file_ops::test_read_and_save_text_file | 接口 | **Windows CRLF**：文件读出 `# Old\r\n`，断言 `== "# Old\n"` | ❌ 平台 |
| 10 | test_workspace_upload_tasks::test_create_upload_tasks_rejects_symlink_escape | 单元 | **Windows 符号链接权限**：`WinError 1314 客户端没有所需特权`（非开发者模式/无 SeCreateSymbolicLink） | ❌ 平台 |
| 11 | test_db::test_create_user_and_session | error | **DB 口令**：该测试硬编码 `postgres:postgres`，而本机口令为 `dev` → InvalidPasswordError | ❌ 配置 |
| 12 | test_create_user_cli::test_create_user_inserts_row | error | 同上：硬编码 `postgres:postgres` | ❌ 配置 |
| 13 | test_create_user_cli::test_create_user_rejects_duplicate | error | 同上 | ❌ 配置 |
| 14 | test_chat_api::test_chat_runner_continues_when_sse_consumer_is_cancelled | 集成 | **待复核**：SSE 消费者取消后，后台 runner 状态停在 `RUNNING`，1s 轮询窗口内未收尾至 `COMPLETED`（3 次复现一致，teardown 日志显示 runner task 被 cancel 未完成） | ⚠️ **需人工复核** |
| 15 | test_claude_runner::test_load_history_returns_latest_ten_messages | 单元 | **契约漂移**：实现 `load_history` 取 `all_msgs[-200:]`（最近 200 条），测试仍断言「最近 10 条」`range(5,15)` | ⚠️ 测试/实现不同步 |

### 3.3 根因汇总

| 根因类别 | 数量 | 性质 | 处置建议 |
|---------|------|------|---------|
| 环境变量泄漏（ANTHROPIC_*） | 3 | 测试隔离不足 | conftest 增加 `delif` 清理，或测试用 `monkeypatch.delenv` |
| 过时测试（企微旧代码） | 11 | 测试未随 V2.2 重构清理 | 删除 `test_wechat_work.py`、`test_departments_sync.py`；修复 `test_sessions_crud.py` 两处 wechat mock |
| Windows 平台差异（路径/CRLF/symlink） | 4 | 跨平台兼容 | 测试改用 `os.sep`/`split()`；CRLF 用 `.replace("\r","")`；symlink 加 `skipif(not hasattr os.symlink)` |
| DB 口令不一致 | 3 | 测试硬编码口令 | 改读 `DATABASE_URL` 环境变量，勿硬编码 `postgres:postgres` |
| **待复核真实缺陷** | 1 | SSE 后台 runner 收尾时序 | 人工排查 `streaming.py` detached runner finalization |
| 契约漂移 | 1 | 历史消息窗口 10→200 | 确认产品意图后同步测试或回退实现 |

### 3.4 验证：环境变量泄漏已证实

对 #1/#2/#3 三条用「剥离全局 ANTHROPIC_* 变量」方式复跑：

```bash
env -u ANTHROPIC_MODEL -u ANTHROPIC_AUTH_TOKEN -u ANTHROPIC_BASE_URL \
  .venv/Scripts/python.exe -m pytest tests/test_config.py::test_settings_defaults \
  tests/test_config.py::test_model_providers_are_discovered_from_env
→ 2 passed
```

证实这 3 条纯粹是 shell 全局变量渗入所致，**非产品缺陷**。

---

## 四、附录 A 冒烟清单（P0 精选）执行结果

测试用例 V1.0 附录 A 列出约 66 条上线前必过 P0 用例。其中**后端可自动化部分**对应的测试文件在全量运行中**全部通过**：

| 冒烟模块 | 对应测试文件 | 用例数 | 结果 |
|---------|------------|------|------|
| 认证与权限 | test_auth_api / test_projects_api（isolation） | 24+18 | ✅ 全过 |
| 项目与 Agent | test_projects_api（auto agent/owner/dept-access） | 18 | ✅ 全过 |
| 业务地图 | test_business_map_api（L1-L4/采纳/版本/过期/隔离） | 22 | ✅ 全过 |
| 营销地图 | test_marketing_map_api（评分/去重/关系/隔离） | 19 | ✅ 全过 |
| 拜访记录 | test_visit_api（CRUD/证据/验证联动） | 9 | ✅ 全过 |
| 对话与 Skill | test_consultant_router / defense / search / skills | 35+15+26+18 | ✅ 全过 |
| 候选区 | test_draft_tools / test_reviews_api | 19+11 | ✅ 全过 |
| 闭环 FLOW | 跨上述文件覆盖（FLOW-007/008/010/011 有后端断言） | — | ✅ 可测部分全过 |
| 安全 NON | test_consultant_defense / test_security | 15+ | ✅ 全过 |
| 系统设置 SYS | test_roles / menus / audit / users_admin | 23+28+19+18 | ✅ 全过 |
| 项目隔离 PISO | test_sessions_project_binding | 18 | ✅ 全过 |
| 跨页面 CROSS | 后端可测联动（验证/偏差/态度/五维） | — | ✅ 全过 |

> **冒烟结论**：附录 A 的后端可自动化子集 **0 失败**。剩余冒烟项为纯 UI（侧边栏渲染、Topbar 徽标、项目选择器同步、页面跳转、雷达图）与 AI 真实执行（WF07/WF09/WF10/WF12 五步生成），需前端 E2E + 真实 LLM 凭证环境补测（见第六章）。

---

## 五、测试用例 → 自动化测试追溯矩阵

由子代理逐文件比对 502 条用例与 74 个测试文件，覆盖统计如下：

### 5.1 按模块覆盖

| 模块 | 用例数 | 有覆盖 | 部分 | 无覆盖 | 覆盖率 |
|------|-------|------|------|-------|-------|
| AUTH | 24 | 19 | 2 | 3 | 87.5% |
| ORG | 21 | 17 | 1 | 3 | 85.7% |
| CUST | 11 | 8 | 1 | 2 | 81.8% |
| PROJ | 24 | 16 | 5 | 3 | 87.5% |
| PERM | 20 | 14 | 6 | 0 | 100% |
| NAV | 6 | 0 | 1 | 5 | 16.7% |
| BMAP | 43 | 16 | 6 | 21 | 51.2% |
| MMAP | 37 | 14 | 5 | 18 | 51.4% |
| VISIT | 29 | 19 | 5 | 5 | 82.8% |
| CHAT | 28 | 18 | 1 | 9 | 67.9% |
| SKILL | 21 | 3 | 3 | 15 | 28.6% |
| CAND | 28 | 10 | 3 | 15 | 46.4% |
| ARCH | 20 | 9 | 1 | 10 | 50.0% |
| FLOW | 13 | 4 | 2 | 7 | 46.2% |
| NON | 22 | 7 | 1 | 14 | 36.4% |
| CROSS | 66 | 6 | 4 | 56 | 15.2% |
| SYS | 70 | 47 | 4 | 19 | 72.9% |
| PISO | 19 | 9 | 0 | 10 | 47.4% |
| **合计** | **502** | **236** | **51** | **215** | **57.2%** |

> 覆盖率 = (有覆盖 + 部分) / 总数。仅计「有覆盖」为 47.0%。

### 5.2 覆盖强弱分布

- **后端 API 覆盖强**：AUTH / ORG / CUST / PROJ / SYS / PISO / VISIT —— CRUD、权限 403/401、项目隔离、边界均有断言。
- **UI 模块近乎零自动化**：NAV（0/6）、CROSS（多为 0/66）、BMAP/MMAP/VISIT/CHAT/CAND/SYS 的 UI 子项 —— 需前端 E2E。
- **AI 工作流执行未测**：SKILL（TC-SKILL-001~012）需真实 Claude 会话跑 WF07/WF09/WF10/WF12 五步；目前仅测 Skill 文件结构与 draft 工具处理器。
- **端到端 FLOW 缺失**：TC-FLOW-002~005 全链路需 AI + 前端栈。
- **安全测试部分缺**：bcrypt、防线指纹过滤已覆盖；SQL 注入（NON-010）、XSS（NON-011）、并发会话隔离（NON-018）无测试。
- **性能测试全缺**：NON-001~005、ORG-020 需压测工具。
- **Topbar 徽标（V2.3 回归重点）零自动化**：CAND-016~022、CROSS-013~017 全为 UI。

### 5.3 无自动化覆盖的 215 条用例归类

| 归类 | 数量 | 测试方式建议 |
|------|------|------------|
| 纯 UI 交互（侧边栏/选择器/徽标/跳转/图表） | ~140 | 前端 E2E（Playwright/Cypress） |
| AI 真实执行（WF02/03/06/07/09/10/12） | ~35 | 集成测试 + 真实 LLM 凭证 |
| 端到端闭环 FLOW | ~7 | 手工冒烟 + E2E |
| 性能/压测 | ~7 | locust/k6 |
| 安全注入（SQL/XSS/并发） | ~4 | 安全扫描 + 专项用例 |
| 其余边界 | ~22 | 补单测 |

---

## 六、前端测试结果

| 项 | 结果 |
|----|------|
| `tsc -b` 类型检查 | ✅ 通过（exit 0） |
| Node 原生 assert 测试（5 文件） | ✅ 5 passed：chatModelSelection / commandMenu / uploadQueuePolicy / workspacePreview / workspaceTaskStatus |
| vitest 依赖测试（4 文件） | ⚠️ 无法运行：`teamSpaceClient` / `teamSpaceMembers` / `teamSpaceNavigation` / `workspaceFileManager` import `vitest`，但 **vitest 未安装**（package.json 无该依赖，亦无 `test` 脚本） |

> **前端测试基础设施缺陷**：4 个测试文件依赖 `vitest`，但项目未安装 vitest，`package.json` 亦无 `test` 脚本（CLAUDE.md 所述 `npm test` 不可用）。需补 `vitest` 依赖或在 CI 中安装。

---

## 七、缺陷与风险登记

### 7.1 待复核真实缺陷（1）

| ID | 描述 | 严重度 | 位置 |
|----|------|-------|------|
| DEFECT-01 | `test_chat_runner_continues_when_sse_consumer_is_cancelled`：SSE 消费者取消后，后台 Claude runner 在 1s 轮询窗口内未收尾至 `COMPLETED`，状态停在 `RUNNING`，teardown 显示 runner task 被 cancel 未完成。3 次复现一致。设计意图（streaming.py:559 注释「SSE 断开只代表订阅结束，不能取消后台 runner」）要求 runner 继续完成。 | 中（待确认） | [streaming.py:460](backend/app/modules/sessions/streaming.py#L460) runner / finalize |

**复核建议**：在真实 LLM 凭证环境下，构造 SSE 断开后观察 `run_state_store` 是否最终 `COMPLETED`；若始终 `RUNNING` 则为真实缺陷（断开恢复链路 `/running/stream` 将无法回放完成态）。

### 7.2 契约漂移（1）

| ID | 描述 | 位置 |
|----|------|------|
| DRIFT-01 | `load_history` 实现取最近 **200** 条（`all_msgs[-200:]`），但测试 `test_load_history_returns_latest_ten_messages` 仍断言「最近 10 条」。需确认产品意图：前端 `/messages` 接口应返回最近 10 还是 200 条？ | [runner.py:78](backend/app/integrations/claude/runner.py#L78) |

### 7.3 测试工程问题（22，非产品缺陷）

- 环境变量泄漏 3 条（conftest 应清理 ANTHROPIC_* 全局变量）
- 过时企微测试 11 条（删 `test_wechat_work.py` / `test_departments_sync.py`，修 `test_sessions_crud.py` wechat mock）
- Windows 平台差异 4 条（路径分隔符 / CRLF / symlink 权限）
- DB 口令硬编码 3 条（改读 `DATABASE_URL` 环境变量）
- 契约漂移 1 条（见 7.2）

### 7.4 覆盖风险

- **V2.3 回归重点（Topbar 徽标找回）零自动化**：TC-CAND-016~022、CROSS-013~017 全为 UI，仅靠手工测试。
- **AI Skill 执行零自动化**：WF07/WF09/WF10/WF12 五步生成未测，无法保证 LLM 产出字段完整性与跨层一致性。
- **安全注入未测**：SQL 注入 / XSS / 并发隔离无自动化断言。

---

## 八、改进建议（优先级排序）

| 优先级 | 建议 | 受益 |
|-------|------|------|
| P0 | 人工复核 DEFECT-01（SSE 断开 runner 收尾），确认是否真实缺陷 | 断开恢复链路可靠性 |
| P0 | 清理 14 条过时企微测试 + 修 2 处 sessions_crud wechat mock | 套件信号纯净，避免误报 |
| P0 | conftest 增加 `monkeypatch.delenv` 清理 ANTHROPIC_* 全局变量 | 消除 3 条环境泄漏误报 |
| P1 | 修 4 条 Windows 平台差异（路径/CRLF/symlink skipif） | 跨平台可运行 |
| P1 | 安装 vitest 并补 `npm test` 脚本，或迁移 4 个 vitest 测试到 node assert | 前端测试可执行 |
| P1 | 确认 DRIFT-01 历史消息窗口 10 vs 200 的产品意图并同步 | 契约一致 |
| P1 | 引入前端 E2E（Playwright）覆盖 NAV/CROSS/Topbar 徽标（V2.3 重点） | 补 UI 自动化最大缺口 |
| P2 | 补 AI Skill 集成测试（真实 LLM 凭证 + WF07/09/10/12 字段断言） | 覆盖核心 AI 产出 |
| P2 | 补 SQL 注入 / XSS / 并发隔离安全测试 | 安全防线 |
| P3 | 补性能压测（NON-001~005、ORG-020） | 性能基线 |

---

## 九、可上线判定

| 判定项 | 结论 |
|-------|------|
| 后端核心业务闭环（注册→登录→建客户→建项目→Agent→对话→采纳→地图→验证→归档） | ✅ 自动化验证通过 |
| 权限隔离（项目隔离 / 越权 403 / admin 放行） | ✅ 自动化验证通过 |
| 系统设置（角色/菜单/审计/用户管理 V2.4） | ✅ 自动化验证通过 |
| 项目隔离对话增强（V2.5 PISO） | ✅ 后端过滤自动化验证通过 |
| 安全防线（道层注入 / 指纹过滤） | ✅ 自动化验证通过 |
| SSE 流式对话基础 | ✅ 写回 session 验证通过；**断开恢复收尾待复核** |
| UI 交互 / Topbar 徽标（V2.3 重点） | ⚠️ 无自动化，需手工/E2E 补测 |
| AI Skill 真实执行 | ⚠️ 无自动化，需真实 LLM 环境补测 |

**综合判定**：后端核心业务与权限/安全防线可上线。**上线前必做**：(1) 复核 DEFECT-01；(2) 手工或 E2E 验证 V2.3 Topbar 徽标找回链路；(3) 真实 LLM 凭证下跑通 WF07/WF09/WF10/WF12 五步生成冒烟。

---

## 附录：执行命令记录

```bash
# 后端全量（约 30 分钟）
cd backend
.venv/Scripts/python.exe -m pytest -q --tb=no -p no:warnings
# → 789 passed, 21 failed, 2 skipped, 3 errors in 1848.97s

# 环境泄漏验证
env -u ANTHROPIC_MODEL -u ANTHROPIC_AUTH_TOKEN -u ANTHROPIC_BASE_URL \
  .venv/Scripts/python.exe -m pytest tests/test_config.py::test_settings_defaults \
  tests/test_config.py::test_model_providers_are_discovered_from_env
# → 2 passed

# 前端类型检查
cd frontend && ./node_modules/.bin/tsc -b   # exit 0

# 前端 Node 单元测试（esbuild 转译后运行）
# → 5 passed / 4 vitest 依赖无法运行
```

> **报告生成**：2026-07-16，基于测试用例 V1.0 全量执行。

---

## 十、修复落地与复验（2026-07-16 补充）

报告第三/七章登记的 21 failed + 3 errors 已**全部修复并复验通过**。全量复跑结果：

```
801 passed, 3 skipped, 0 failed, 0 errors（1913s）
```

> 相比修复前（789 passed / 21 failed / 2 skipped / 3 errors）：删除 11 条企微死测试（收集数 815→804），**失败与错误清零**；新增 1 个 skip 为 Windows 符号链接权限用例（符合预期）。

### 10.1 逐项修复

| # | 原问题 | 根因 | 修复方式 | 涉及文件 |
|---|--------|------|---------|---------|
| 1-3 | 环境变量泄漏（config/model_settings 3 条） | shell 全局 `ANTHROPIC_*` 渗入 pytest | conftest 新增 autouse `_isolate_anthropic_env`，每条测试前剥离旧 `ANTHROPIC_AUTH_TOKEN/BASE_URL/MODEL` | [conftest.py](../../backend/tests/conftest.py) |
| 4-6 | DB 口令硬编码（3 条 error） | 测试夹具写死 `postgres:postgres`，本机为 `dev` | 提取 `TEST_DATABASE_URL` 单一来源；两夹具改用 `test_database_url` 复用 | conftest.py / test_db.py / test_create_user_cli.py |
| 4-6 续 | （口令修对后暴露）`NoReferencedTableError: chat_sessions.project_id` | 两夹具只 reload 5 个模型，`Base.metadata` 缺 `projects` 等表 | conftest 新增 `isolated_db_module` 夹具，重载全部 21 个模型子模块 + 干净 schema | conftest.py / test_db.py / test_create_user_cli.py |
| 7-8 | 过时企微测试（wechat_work 5 + departments 6 + sessions_crud 1） | V2.2 已弃用企微，源码全文注释，测试未清理 | 删除 test_wechat_work.py / test_departments_sync.py；重写 `test_session_isolation_between_users` 改用自建注册登录 | （删除 2 文件）/ test_sessions_crud.py |
| 9 | DEFECT-01：SSE 断开后 runner 不收尾（**经复盘为测试隔离缺陷，非产品缺陷**） | 首条消息触发 `_make_semantic_title`，未 mock 且读取真实 `.env` 的 DeepSeek token 真实联网 10s，阻塞 `finish_run_state`，超出 1s 轮询窗口 | SSE 用例补 mock `generate_chat_completion` | test_chat_api.py |
| 10 | DRIFT-01：消息窗口 10 vs 200 契约漂移 | 实现取最近 200 条（生产行为），测试仍断言 10 | runner.py 提取 `HISTORY_WINDOW=200` 常量；测试改用常量验证窗口截断（喂 250 条断言末 200） | runner.py / test_claude_runner.py |
| 11-13 | Windows 路径分隔符（messages/sessions 2 条） | `str(ws)` 在 Windows 为反斜杠 | 断言前 `.replace("\\","/")` 归一化为 POSIX | test_messages_api.py / test_sessions_crud.py |
| 14 | Windows CRLF（workspace_file_ops 1 条） | 文本写入引入 `\r\n` | 断言前 `.replace("\r","")` 归一化 | test_workspace_file_ops.py |
| 15 | Windows 符号链接权限（upload_tasks 1 条） | WinError 1314 无 SeCreateSymbolicLink | 构造符号链接失败时 `pytest.skip`（仅 WinError 1314） | test_workspace_upload_tasks.py |

### 10.2 结论修订

- 第七章 DEFECT-01 由「待复核真实缺陷」**降级并关闭**为「测试隔离缺陷」——产品 SSE 断开恢复链路本身正确（runner 在 finalize 阶段调用语义标题生成，完成后正常置 COMPLETED），仅测试未隔离该网络调用。
- 第七章 DRIFT-01 由「契约漂移」**关闭**——经确认 `load_history` 返回最近 200 条为既定生产行为，已用 `HISTORY_WINDOW` 常量显式化契约并同步测试。
- 第九章「可上线判定」中「上线前必做：复核 DEFECT-01」**已落实**，不再阻塞；其余（V2.3 Topbar 徽标 E2E、真实 LLM 跑 WF07/09/10/12）仍需在对应环境补测。
