# AI 顾问作战台 · 开发计划拆解

> 基于 2026-07-08 需求访谈产出，对齐需求规格说明书 V2.1
> 前置条件：清库重来（二开底座），企微认证注释保留

---
如何用 Claude Code 执行本计划

每张 `CC-XX` 任务卡都是**可直接喂给 Claude Code 的 prompt 骨架**。推荐工作流：

1. **按依赖顺序逐卡执行**，不要跳序。
2. **每卡执行步骤**：把卡的"Claude 输入 + 目标文件 + 验收"贴给 Claude Code → Claude 实现 → 跑"验证命令" → 人工对照"验收"勾掉。
3. **每卡完成后提交一次**（遵循 `feat:/fix:` 规范），保持可回滚。
4. **到达里程碑门禁**（第十二章）才进下一 Phase。

> 任务卡字段说明：
> - **Claude 输入**：Claude 实现前需要读的基准文档章节 / 参考代码
> - **目标文件**：Claude 将创建或修改的具体路径
> - **验收**：可勾选的完成标准
> - **验证命令**：客观判定通过的命令（`pytest` / `npm run build` / 手动跑）

## 总览：5 个 Phase，约 10-14 周

| Phase | 名称 | 周期 | 核心交付 |
|-------|------|------|---------|
| P1 | 地基重构 | 2-3 周 | 认证体系 + 组织架构 + 客户/项目模型 + 权限中间件 |
| P2 | 数据模型与 API | 2-3 周 | 全部业务实体 + CRUD API + 草稿/采纳/版本机制 |
| P3 | Skill 与 Plugin | 2-3 周 | 7 Skill + 3 Plugin + Tool Use 机制 + 意图路由 |
| P4 | 前端页面 | 2-3 周 | 业务地图/营销地图/拜访记录三个真实页面 + 对话采纳交互 |
| P5 | 联调与打磨 | 1-2 周 | 端到端联调 + 审批流程 + 版本回溯 + 角色关系图 |

---

## Phase 1：地基重构（2-3 周）

### M1.1 认证体系重构

**目标**：从企微 SSO 切换为自建注册登录体系

| 任务 | 说明 | 产出 |
|------|------|------|
| M1.1.1 注释企微认证代码 | `app/modules/auth/wechat_work.py` + `departments.py` 注释保留，不删除 | 企微代码归档 |
| M1.1.2 User 模型扩展 | 新增 `phone`（手机号，唯一）、`password_hash`、`status`（pending_approval/active/disabled）、`registration_source`（self_register/admin_create）字段 | 迁移脚本 + 模型更新 |
| M1.1.3 注册 API | `POST /api/auth/register`（手机号+密码 或 用户名+密码），默认 status=pending_approval | API + 测试 |
| M1.1.4 登录 API | `POST /api/auth/login`（手机号/用户名 + 密码），返回 session cookie | API + 测试 |
| M1.1.5 登出 API | `POST /api/auth/logout`，清除 session | API + 测试 |
| M1.1.6 管理员审批 API | `GET /api/admin/pending-users`、`POST /api/admin/approve-user/{id}`、`POST /api/admin/reject-user/{id}` | API + 测试 |
| M1.1.7 前端登录页重构 | 替换企微二维码为手机号/用户名+密码登录表单；新增注册页面；新增管理员审批页面 | LoginPage + RegisterPage + UserApprovalPage |
| M1.1.8 前端认证逻辑恢复 | App.tsx 中删除 mock 认证绕过，恢复真实登录流程 | App.tsx 更新 |

### M1.2 组织架构

**目标**：自建三级组织架构（公司→部门→小组），后台维护

| 任务 | 说明 | 产出 |
|------|------|------|
| M1.2.1 Organization 模型 | `organizations` 表：id, name, type（company/department/group）, parent_id, head_user_id, sort_order, created_at, updated_at | 模型 + 迁移 |
| M1.2.2 User-Organization 关联 | `user_organizations` 表：user_id, organization_id, position_title, is_primary | 模型 + 迁移 |
| M1.2.3 组织 CRUD API | `GET/POST/PUT/DELETE /api/admin/organizations`，支持树形结构查询 | API + 测试 |
| M1.2.4 批量导入 API | `POST /api/admin/organizations/import`（CSV/Excel），校验+去重+批量创建 | API + 测试 |
| M1.2.5 前端组织管理页 | 后台设置中新增"组织架构"管理页：树形展示 + 拖拽排序 + 批量导入 | OrganizationPage |

### M1.3 客户与项目模型

**目标**：建立 Customer 1:N Project 数据模型，项目权限控制

| 任务 | 说明 | 产出 |
|------|------|------|
| M1.3.1 Customer 模型 | 复用/重建 `customers` 表：id, name, industry, scale, region, description, created_by, created_at, updated_at | 模型 + 迁移 |
| M1.3.2 Project 模型 | `projects` 表：id, customer_id, name, agent_id, project_type, fde_stage, status, owner_id, description, objectives, start_date, end_date, created_by, created_at, updated_at | 模型 + 迁移 |
| M1.3.3 ProjectMember 模型 | `project_members` 表：id, project_id, user_id, role（owner/deputy）, joined_at | 模型 + 迁移 |
| M1.3.4 ProjectDepartmentAccess 模型 | `project_department_access` 表：id, project_id, organization_id, granted_by, granted_at | 模型 + 迁移 |
| M1.3.5 Customer CRUD API | `/api/customers` 全套 CRUD，admin 看全部，user 看自己参与的客户关联项目 | API + 测试 |
| M1.3.6 Project CRUD API | `/api/projects` 全套 CRUD + 成员管理 + 部门授权 | API + 测试 |
| M1.3.7 项目创建自动生成 Agent | 创建项目时自动创建 Agent 实例，绑定标准 Skill/Plugin 集合 | 服务层逻辑 + 测试 |
| M1.3.8 权限中间件 | `require_project_member`、`require_project_owner` 依赖注入，项目级数据隔离 | 中间件 + 测试 |
| M1.3.9 前端项目选择器 | 顶部项目选择器组件（客户→项目级联下拉），全局状态管理 | ProjectSelector 组件 |

### M1.4 导航重构

| 任务 | 说明 | 产出 |
|------|------|------|
| M1.4.1 Sidebar 更新 | 作战台分组：对话/业务地图/营销地图/拜访记录；文件分组：个人空间/团队空间；管理分组：智能体/组织架构/用户管理/设置 | SidebarVariantA 更新 |
| M1.4.2 路由更新 | ViewName 类型更新，新增 organization/userApproval 等视图 | types/index.ts + App.tsx |

---

## Phase 2：数据模型与 API（2-3 周）

### M2.1 业务地图数据

| 任务 | 说明 | 产出 |
|------|------|------|
| M2.1.1 BusinessMapObject 模型 | 按 V2.1 规格实现：objectId, projectId, level, name, parentId, mapType, verificationStatus, linkedHypothesisId, payload(JSONB), reviewStatus, reviewedBy, reviewedAt, generatedByAI, createdBy, createdAt, updatedAt, isPublic, sharedWith, sensitivityLevel | 模型 + 迁移 |
| M2.1.2 PreAnalysis 模型 | 项目级前置分析：id, projectId, industryValueChain, customerPosition, industryTrends, strategicPositioning, digitalizationDrivers, createdBy, createdAt, updatedAt | 模型 + 迁移 |
| M2.1.3 BusinessMapVersion 模型 | 项目级版本快照：id, projectId, versionNumber, snapshotData(JSONB), changeDescription, createdBy, createdAt | 模型 + 迁移 |
| M2.1.4 BusinessMapDraft 模型 | 草稿区：id, projectId, draftData(JSONB), sourceSessionId, createdBy, createdAt, expiresAt, status（active/adopted/expired） | 模型 + 迁移 |
| M2.1.5 BusinessMap CRUD API | `/api/projects/{id}/business-map/objects` 全套 CRUD + 按层级/地图类型筛选 + 关联证据查询 | API + 测试 |
| M2.1.6 PreAnalysis API | `/api/projects/{id}/business-map/pre-analysis` CRUD | API + 测试 |
| M2.1.7 草稿区 API | `/api/projects/{id}/business-map/drafts` CRUD + 7天过期检查 + 采纳（draft→正式数据+版本快照） | API + 测试 |
| M2.1.8 版本管理 API | `/api/projects/{id}/business-map/versions` 列表 + 详情 + 回滚 | API + 测试 |
| M2.1.9 五维健康计算服务 | 根据业务地图节点数据 + 总纲文档规则，自动计算五维健康评分；支持手动覆盖 | 服务层 + 测试 |

### M2.2 营销地图数据

| 任务 | 说明 | 产出 |
|------|------|------|
| M2.2.1 StakeholderCard 模型 | 按 V2.1 规格实现完整字段：客观层+主观层+behaviors(JSONB)+stanceChangeLog(JSONB)+reviewStatus 等 | 模型 + 迁移 |
| M2.2.2 StakeholderRelation 模型 | 角色关联关系：id, projectId, fromCardId, toCardId, relationType（reports_to/influences/collaborates/opposes）, description, createdBy, createdAt | 模型 + 迁移 |
| M2.2.3 TalkScript 模型 | 话术：id, stakeholderCardId(nullable), projectId, roleType, scenario, content(Markdown), sourceCustomerQuote, isTemplate, createdBy, createdAt, updatedAt | 模型 + 迁移 |
| M2.2.4 KnowledgeBase 模型 | 知识库：id, projectId, category（role_recognition/behavior_quick_ref/onboarding_guide）, title, content(Markdown+富文本), createdBy, createdAt, updatedAt | 模型 + 迁移 |
| M2.2.5 StakeholderCard CRUD API | `/api/projects/{id}/stakeholder-cards` 全套 CRUD + 按部门/角色类型/立场筛选 | API + 测试 |
| M2.2.6 StakeholderRelation CRUD API | `/api/projects/{id}/stakeholder-relations` CRUD + 关系网络图数据接口 | API + 测试 |
| M2.2.7 TalkScript CRUD API | `/api/projects/{id}/talk-scripts` CRUD + 按角色类型/场景筛选 | API + 测试 |
| M2.2.8 KnowledgeBase CRUD API | `/api/projects/{id}/knowledge-base` CRUD + 按类别筛选 | API + 测试 |
| M2.2.9 态度变化自动记录 | 新证据关联角色卡时，自动生成 stanceChangeLog 条目 | 服务层逻辑 + 测试 |

### M2.3 拜访记录数据

| 任务 | 说明 | 产出 |
|------|------|------|
| M2.3.1 VisitRecord 模型 | 按 V2.1 规格实现：visitRecordId, projectId, visitDate, visitType, participantsOur, participantsClient, location, duration, summary, nextSteps, keyTakeaways, evidenceCount, verifiedHypotheses, relatedCardIds, reviewStatus 等 | 模型 + 迁移 |
| M2.3.2 EvidenceSource 模型 | 证据：evidenceId, visitRecordId, projectId, evidenceType, strength, strengthNote, content, sourceRoleId, sourceRoleName, relatedHypothesisId, relatedHypothesisName, reviewStatus 等 | 模型 + 迁移 |
| M2.3.3 VisitRecord CRUD API | `/api/projects/{id}/visit-records` CRUD + 时间线排序 + 按类型/角色筛选 | API + 测试 |
| M2.3.4 EvidenceSource CRUD API | `/api/projects/{id}/evidence-sources` CRUD + 多维筛选（类型/强度/角色/关联假设） | API + 测试 |
| M2.3.5 证据验证联动 | 证据关联假设节点时，AI 建议验证状态 + 人工确认 → 更新 verificationStatus + 自动新增偏差池条目 | 服务层逻辑 + 测试 |

### M2.4 采纳与审批机制

| 任务 | 说明 | 产出 |
|------|------|------|
| M2.4.1 采纳 API | `POST /api/projects/{id}/adopt`：Owner 直接采纳；Deputy 采纳进入 pending_review | API + 测试 |
| M2.4.2 审批 API | `GET /api/projects/{id}/pending-reviews`：Owner 查看待审批列表；`POST /api/projects/{id}/reviews/{id}/approve` / `reject` | API + 测试 |
| M2.4.3 采纳后数据流转 | 采纳 → 草稿数据写入正式表 → 生成版本快照 → 清除草稿 → 对应数据页面可见 | 服务层逻辑 + 测试 |

---

## Phase 3：Skill 与 Plugin（2-3 周）

### M3.1 Tool Use 基础框架

| 任务 | 说明 | 产出 |
|------|------|------|
| M3.1.1 Tool Use 注册机制 | 扩展 `guard.py` 工具钩子，支持注册自定义 Tool（save_business_map_draft, save_stakeholder_card_draft, save_visit_record_draft） | 框架代码 + 测试 |
| M3.1.2 Tool JSON Schema 定义 | 为每个 Skill 的结构化输出定义 JSON Schema，AI 调用 Tool 时传入校验 | Schema 文件 |
| M3.1.3 Tool 调用拦截与存储 | AI 调用 save_xxx_draft → 后端拦截 → Schema 校验 → 存入草稿表 → SSE 推送"待采纳"状态给前端 | 拦截逻辑 + 测试 |
| M3.1.4 前端采纳交互 | 对话消息中显示"待采纳"卡片（结构化数据预览 + 采纳/驳回按钮）→ 点击采纳调 API | ChatWorkspace 组件更新 |

### M3.2 7 个 Skill 编写

| 任务 | 说明 | 产出 |
|------|------|------|
| M3.2.1 WF02 consultant-upload | 资料上传与归档 Skill：SKILL.md + 结构化输出 Schema | Skill 文件 |
| M3.2.2 WF03 consultant-gap-check | 信息缺口识别辅助 Skill：SKILL.md（被 WF06/WF07 自动前置调用） | Skill 文件 |
| M3.2.3 WF06 consultant-visit-plan | 拜访前方案生成 Skill：SKILL.md + 结构化输出 Schema | Skill 文件 |
| M3.2.4 WF07 consultant-hypothesis-map | 假设地图分步生成 Skill：SKILL.md + L1-L4 字段契约 + 5 Step 强制定步 + 搜索公开信息指令 + 结构化输出 Schema | Skill 文件 |
| M3.2.5 WF09 consultant-interview | 拜访纪要结构化 Skill：SKILL.md + 证据抽取维度 + 结构化输出 Schema | Skill 文件 |
| M3.2.6 WF10 consultant-verify | 假设验证与现状更新 Skill：SKILL.md + 证据匹配逻辑 + 现状节点创建规则 + 结构化输出 Schema | Skill 文件 |
| M3.2.7 WF12 consultant-stakeholder | 营销地图角色卡生成 Skill：SKILL.md + 客观/主观层字段 + 三维度评分公式 + 角色去重逻辑 + 结构化输出 Schema | Skill 文件 |
| M3.2.8 Skill 产出字段全量核对（回归补充） | 逐个核对 WF07/WF09/WF10/WF12 的 SKILL.md 产出字段 vs §5.2/原型完整性，缺则补；**WF12 必补 `subjective_layer.confidence`（置信度）**——§5.2 已定义但现 Skill 字段清单与示例均遗漏，导致 AI 不产出。产出《Skill 字段核对清单》 | Skill 文件修订 + 核对清单 |

### M3.3 3 个 Plugin 实施

| 任务 | 说明 | 产出 |
|------|------|------|
| M3.3.1 consultant-router | 意图路由 Plugin：Hook 脚本（拦截用户输入 → LLM 分类 → 路由到对应 Skill）+ 7 类意图标签 + 三级路由（LLM分类→关键词兜底→用户确认）+ IntentRoutingLog 记录 | Plugin 目录 + 测试 |
| M3.3.2 consultant-search | MCP 搜索工具集 Plugin：search_web / search_company_registry / fetch_webpage 三个 MCP Tool + 复用 web_research 管线 | Plugin 目录 + 测试 |
| M3.3.3 consultant-defense | 防线 Plugin：道层 System Prompt 注入 Hook + PostOutputFilter Hook（确定性指纹匹配） | Plugin 目录 + 测试 |

### M3.4 对话集成

| 任务 | 说明 | 产出 |
|------|------|------|
| M3.4.1 WF chip 按钮 | 对话底部输入区新增 5 个工作流 chip（生成假设地图/生成拜访前方案/整理拜访纪要/验证假设/营销地图） | ChatWorkspace 组件更新 |
| M3.4.2 项目级会话关联 | 会话创建时关联 projectId，会话内自动加载项目 Agent（含 Skill/Plugin 绑定） | 会话服务层更新 |
| M3.4.3 Chat 调整循环 | 采纳前支持自然语言修改 → AI 增量更新草稿 → 展示 diff → 再次询问采纳 | 对话流程逻辑 |

---

## Phase 4：前端页面（2-3 周）

### M4.1 业务地图页面（替换原型）

| 任务 | 说明 | 产出 |
|------|------|------|
| M4.1.1 页面骨架 | 顶部项目选择器 + 统计栏 + 子视图切换（假设地图/现状地图/偏差池/前置分析/五维健康） | BusinessMapPage 重写 |
| M4.1.2 L1-L4 树形展示 | 左侧树形结构（L1横向→L2展开→L3展开→L4），右侧详情面板（按层级差异化字段） | 树形组件 + 详情面板 |
| M4.1.3 假设地图视图 | mapType=hypothesis 节点展示，含置信度/来源类型标签 | 子视图组件 |
| M4.1.4 现状地图视图 | mapType=current 节点展示，含 verificationStatus + linkedHypothesisId 可点击跳转 | 子视图组件 |
| M4.1.5 偏差池视图 | verificationStatus=推翻 的节点列表，假设 vs 现状逐条对比，红色高亮 | 子视图组件 |
| M4.1.6 前置分析视图 | 项目级 PreAnalysis 展示与编辑 | 子视图组件 |
| M4.1.7 五维健康视图 | L1 雷达图 + L2/L3 逐域评分表 + 点击跳转节点详情 | 子视图组件 |
| M4.1.8 节点 CRUD | 手动新增/编辑/删除节点（直接进正式库） | 编辑表单 + API 调用 |
| M4.1.9 版本管理 | 版本列表 + 历史版本查看 + 回滚操作 | 版本面板 |
| M4.1.10 关联证据展示 | 节点详情面板中展示关联证据列表，可跳转拜访记录页面 | 关联组件 |

### M4.2 营销地图页面（替换原型）

| 任务 | 说明 | 产出 |
|------|------|------|
| M4.2.1 页面骨架 | 顶部项目选择器 + 统计栏 + 六视图切换 | MarketingMapPage 重写 |
| M4.2.2 组织架构图视图 | 树形结构，标注汇报关系、关键岗位 | 子视图组件 |
| M4.2.3 决策链角色表视图 | 表格 + 影响力条形图 + 综合评分 + 等级，按影响力排序 | 子视图组件 |
| M4.2.4 角色-立场矩阵视图 | 横轴立场 × 纵轴影响力，散点气泡图（气泡大小=参与度） | 子视图组件 |
| M4.2.5 采购流程时间线视图 | 五阶段时间线 + 手动填写 | 子视图组件 |
| M4.2.6 角色卡牌视图 | 左侧角色列表 + 中间详情卡（5个子Tab：客观/主观/行为/态度历史/话术）+ 右侧关联面板；详情字段须覆盖 §5.2 全量（客观层 7 / 主观层全 / 行为 / 态度历史），**含 `confidence` 置信度**（依赖 M3.2.8 Skill 补产出） | 子视图组件 |
| M4.2.7 知识库视图 | 三板块（角色识别/行为速查/入职指南），支持丰富格式 | 子视图组件 |
| M4.2.8 角色关系网络图 | 节点=角色卡，边=关系（4种类型不同颜色/线型），可交互 | 关系图组件（可用 D3/ReactFlow） |
| M4.2.9 角色 CRUD | 手动新增/编辑/删除角色卡 + 关系编辑 | 编辑表单 + API 调用 |
| M4.2.10 话术库管理 | 按角色类型×场景组织，支持 Markdown 编辑 | 话术编辑组件 |

### M4.3 拜访记录页面（替换原型）

| 任务 | 说明 | 产出 |
|------|------|------|
| M4.3.1 页面骨架 | 顶部项目选择器 + 统计栏 + 左侧拜访列表 + 右侧证据筛选面板 | VisitRecordsPage 重写 |
| M4.3.2 拜访记录列表 | 时间倒序卡片列表，展示日期/类型/参与人/证据数/验证数 | 列表组件 |
| M4.3.3 拜访记录详情 | 展开查看完整信息 + 证据列表 + 关联角色卡 + 关联假设 | 详情组件 |
| M4.3.4 证据筛选面板 | 按类型/强度/角色多维筛选 + 筛选结果列表 | 筛选组件 |
| M4.3.5 拜访记录 CRUD | 手动新增/编辑/删除 + 审批流程 | 编辑表单 + API 调用 |
| M4.3.6 证据关联交互 | 证据→点击跳转业务地图节点 / 点击跳转角色卡 | 关联跳转组件 |

### M4.4 对话页面更新

| 任务 | 说明 | 产出 |
|------|------|------|
| M4.4.1 项目选择器集成 | 对话页顶部新增项目选择器，选择项目后自动关联 Agent | ChatWorkspace 更新 |
| M4.4.2 待采纳卡片渲染 | AI Tool Use 产出渲染为结构化预览卡片 + 采纳/驳回按钮 | 消息组件更新 |
| M4.4.3 WF chip 集成 | 底部输入区 5 个 chip 按钮 | 输入区组件更新 |
| M4.4.4 审批提醒 Banner | Deputy 采纳后 Owner 在对话 Banner 收到待审核提醒 | Banner 组件 |
| M4.4.5 待采纳卡片全字段 + 待采纳徽标兜底（回归补充） | ① 三类草稿「待采纳」卡片改为"关键字段默认展开 + 其余折叠可展开"全字段展示（角色卡 / 业务地图 / 拜访，对齐 §7.1 第 9 条）；② Topbar 新增项目级"N 待采纳"红点徽标，展开轻量列表可直接采纳或跳回原对话（§7.1 第 8 条）；③ **后端微调**：`tools.py` 三类草稿 `preview` payload 丰富（角色卡由 `{name, role_type}` 扩为关键字段集；role_type 出中文标签）+ stakeholder/visit 草稿补存 `source_session_id` 以支持徽标"跳回原对话" | ChatWorkspace + Topbar 更新 + `tools.py` preview 扩展 |

---

## Phase 5：联调与打磨（1-2 周）

### M5.1 端到端联调

| 任务 | 说明 | 产出 |
|------|------|------|
| M5.1.1 完整流程联调 | 注册→登录→创建客户→创建项目→对话生成假设地图→采纳→业务地图页面可见→生成拜访方案→整理纪要→验证假设→更新现状→生成角色卡 | 联调通过 |
| M5.1.2 权限联调 | Owner/Deputy 权限差异验证；项目隔离验证；部门授权验证 | 权限测试通过 |
| M5.1.3 草稿过期测试 | 7天过期提醒 + 过期后草稿不可采纳 | 测试通过 |

### M5.2 审批流程完善

| 任务 | 说明 | 产出 |
|------|------|------|
| M5.2.1 WF07 审核流程 | Deputy 提交 → Owner 审核 → 通过发布 / 驳回退回 | 完整流程 |
| M5.2.2 拜访记录审批 | 拜访记录采纳需审批 | 完整流程 |
| M5.2.3 审核通知 | Owner 收到待审核提醒（对话 Banner + 通知） | 通知机制 |

### M5.3 版本回溯

| 任务 | 说明 | 产出 |
|------|------|------|
| M5.3.1 版本快照生成 | 每次采纳自动生成项目级版本快照 | 功能验证 |
| M5.3.2 版本对比 | 查看历史版本 + 与当前版本 diff | 功能验证 |
| M5.3.3 版本回滚 | 回滚到历史版本，替换当前数据 | 功能验证 |

### M5.4 角色关系图

| 任务 | 说明 | 产出 |
|------|------|------|
| M5.4.1 关系网络图渲染 | D3/ReactFlow 实现交互式关系图 | 组件完成 |
| M5.4.2 关系图交互 | 点击节点→跳转角色卡详情；悬停边→显示关系描述 | 交互完成 |

### M5.5 其他打磨

| 任务 | 说明 | 产出 |
|------|------|------|
| M5.5.1 角色去重 | person_disambiguation 候选生成 + 用户确认 | 功能完成 |
| M5.5.2 态度变化追踪 | 新证据关联角色卡时自动生成 stanceChangeLog | 功能验证 |
| M5.5.3 对象公开机制 | 标记"完全公开"→团队空间可见；"共享给"→指定用户可见 | 功能完成 |
| M5.5.4 个人空间项目筛选 | 顶部项目筛选框 + 按项目过滤文件 | 功能完成 |
| M5.5.5 五维健康自动计算 | 对话更新地图数据后触发五维健康重新评估 | 功能验证 |

---

## 依赖关系图

```
P1 地基重构
  ├── M1.1 认证 ──→ M1.4 导航重构
  ├── M1.2 组织架构 ──→ M1.3 项目权限（部门授权依赖组织架构）
  └── M1.3 客户/项目 ──→ P2 全部（数据模型依赖项目模型）

P2 数据模型与 API
  ├── M2.1 业务地图 ──→ M2.4 采纳机制（草稿依赖业务地图模型）
  ├── M2.2 营销地图 ──→ M2.3 拜访记录（证据关联角色卡）
  └── M2.3 拜访记录 ──→ M2.1（证据验证联动依赖业务地图模型）

P3 Skill 与 Plugin
  ├── M3.1 Tool Use 框架 ──→ M3.2 Skill（Skill 依赖 Tool Use 机制）
  ├── M3.2 Skill ──→ M3.4 对话集成
  └── M3.3 Plugin ──→ M3.4 对话集成

P4 前端页面
  ├── M4.1 业务地图 ──→ 依赖 P2 M2.1 API
  ├── M4.2 营销地图 ──→ 依赖 P2 M2.2 API
  ├── M4.3 拜访记录 ──→ 依赖 P2 M2.3 API
  └── M4.4 对话更新 ──→ 依赖 P3 M3.1 + M3.4

P5 联调打磨 ──→ 依赖 P1-P4 全部完成
```

---

## 风险与注意事项

1. **清库影响**：P1 完成后需清库，所有现有测试数据丢失。建议 P1 完成后立即清库，后续 Phase 在新库上开发
2. **企微代码保留**：注释而非删除，确保未来可恢复。建议在代码中加 `# DEPRECATED: 企微认证，保留以备未来扩展` 标记
3. **Skill Prompt 质量**：7 个 Skill 的 SKILL.md 质量很大，建议 M3.2 按优先级排序：WF07（假设地图）> WF09（拜访纪要）> WF12（角色卡）> WF10（验证）> WF06（拜访方案）> WF02（上传）> WF03（缺口检查）
4. **角色关系图**：D3/ReactFlow 选型需在 P4 开始前确定，建议用 ReactFlow（更易集成 React 生态）
5. **五维健康计算**：评分规则需对齐总纲文档，建议 M2.1.9 先实现简单规则版本，P5 打磨时优化
6. **前端页面替换策略**：三个原型页面（BusinessMapPage/MarketingMapPage/VisitRecordsPage）直接重写，不保留原型代码。原型代码可在 git 历史中回溯
